#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int t;cin>>t;
	while(t--)
	{
	    int x,y;
	    cin>>x>>y;
	    if(x-y==0)
	        cout<<(y*2)-1<<endl;
	    else if(y==0)
	        cout<<x<<endl;
	   else
	   cout<<(y*2)+(x-y)<<endl;
	}
	return 0;
}
