#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int T;
	int N;
    cin >> T;
	while(T--){
        cin >> N;
        int result = N - floor(log2(N)) - 1;
        cout << result << endl;
	}
	return 0;
}
