 int sum=0;
    // Complete this function
    int sumOfDigits(int n)
    {
        //Your code here
        if(n>0)
        {
            sum=sum+(n%10);
            sumOfDigits(n/10);
        }
       return sum;
    }
